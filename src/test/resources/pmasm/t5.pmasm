MOV R1, #2        
MOV R2, #10        
BL @POWER
SUBI R0, R0, #1024 
EXIT

.POWER
CMPI R2, #0
BEQ @POWER_EXP_ZERO
MOV R0, #1
MOV R3, R2

.POWER_LOOP
MULI R0, R0, R1
SUBI R3, R3, #1
CMPI R3, #0
BGT @POWER_LOOP
B @POWER_RETURN

.POWER_EXP_ZERO
MOV R0, #1

.POWER_RETURN
MOV R1, DL
POP DL
B R1
EXIT